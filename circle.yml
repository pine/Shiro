machine:
  ruby:
    version: 2.2.2

dependencies:
  pre:
    - .circle/install-crenv.bash
    - crenv --version
    - .circle/install-crystal.bash
    - crystal --version

  override:
    - crystal deps

  cache_directories:
    - .crystal
    - .deps
    - ~/.crenv

test:
  override:
    - crystal spec

deployment:
  production:
    branch: master
    commands:
      - echo deploy
