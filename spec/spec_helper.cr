require "spec"

# Dir.glob("sql/*.sql") do |file|
#   queries = File.read(file).split(";")
#   queries.each do |query|
#     query = query.chomp
#     conn.query query if query.size > 0
#   end
# end
